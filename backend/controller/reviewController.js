const Review = require("../model/reviewmodel");

const create = async (req, res) => { try { const data = await Review.create(req.body); res.status(201).json({ success: true, message: "Review created successfully", data }); } catch (error) { res.status(400).json({ success: false, message: "Review creation failed", error: error.message }); } };
const list = async (req, res) => { try { const query = req.query.status ? { status: req.query.status } : {}; const data = await Review.find(query).sort({ createdAt: -1 }); res.json({ success: true, data }); } catch (error) { res.status(400).json({ success: false, message: "Review list failed", error: error.message }); } };
const details = async (req, res) => { try { const data = await Review.findById(req.params.review_id); if (!data) return res.status(404).json({ success: false, message: "Review not found" }); res.json({ success: true, data }); } catch (error) { res.status(400).json({ success: false, message: "Review details failed", error: error.message }); } };
const update = async (req, res) => { try { const data = await Review.findByIdAndUpdate(req.params.review_id, req.body, { new: true, runValidators: true }); if (!data) return res.status(404).json({ success: false, message: "Review not found" }); res.json({ success: true, message: "Review updated successfully", data }); } catch (error) { res.status(400).json({ success: false, message: "Review update failed", error: error.message }); } };
const destroy = async (req, res) => { try { const data = await Review.findByIdAndDelete(req.params.review_id); if (!data) return res.status(404).json({ success: false, message: "Review not found" }); res.json({ success: true, message: "Review deleted successfully" }); } catch (error) { res.status(400).json({ success: false, message: "Review delete failed", error: error.message }); } };
module.exports = { create, list, details, update, destroy };
